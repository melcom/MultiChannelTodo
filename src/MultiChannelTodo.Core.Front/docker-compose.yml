version: '2'

services:
  multichanneltodo.core.front:
    image: phgache/multichanneltodofront
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - NetCore_Env=${NETCORE_ENV}
      - NetCore_ApiPath=${NETCORE_APIPATH}
